import styled from "styled-components";

export const Container = styled.span`
    font-size: 20px;
`;

export const Input = styled.input`
    text-align: center;
    color: var(--THEME_COLOR_001);
    background-color: var(--THEME_COLOR_002);
    width: 65px;
    height: 40px;
    border-radius: 8px;
    border: 1px solid var(--THEME_COLOR_001);
`;