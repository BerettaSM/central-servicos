import styled from 'styled-components';

import {  UserCircle } from 'styled-icons/fa-solid';

export const AttendantPicture = styled(UserCircle)`
    color: var(--THEME_COLOR_005);
    width: 35px;
`;
